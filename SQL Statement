# SQL Statement / Query

-- SQL Query: Select customers from Italy with a score below 500

-- Clauses: SELECT
-- Keywords: FROM, WHERE
-- Identifiers: first_name, last_name
-- Operators: =, <

SELECT first_name, last_name
FROM customers
WHERE country = 'Italy' AND score < 500;

